# Email Viewer Environment Configuration
# Copy this file to .env and update with your actual values

# Upload Configuration
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
SESSION_TIMEOUT=3600       # 1 hour in seconds
TEMP_FILE_RETENTION=3600   # 1 hour in seconds

# Optional Database Configuration
# Uncomment and configure if you want to use database features
# DB_HOST=localhost
# DB_NAME=email_viewer
# DB_USER=your_db_user
# DB_PASS=your_db_password

# Optional: Email Storage Database Schema
# If using database, run this SQL to create tables:
#
# CREATE TABLE IF NOT EXISTS email_uploads (
#     id VARCHAR(50) PRIMARY KEY,
#     filename VARCHAR(255) NOT NULL,
#     file_path VARCHAR(500) NOT NULL,
#     from_email VARCHAR(255),
#     to_email VARCHAR(255),
#     subject TEXT,
#     upload_date DATETIME DEFAULT CURRENT_TIMESTAMP,
#     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
#     INDEX idx_created_at (created_at),
#     INDEX idx_from_email (from_email),
#     INDEX idx_subject (subject(100))
# ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
#
# CREATE TABLE IF NOT EXISTS email_attachments (
#     id VARCHAR(50) PRIMARY KEY,
#     email_id VARCHAR(50),
#     filename VARCHAR(255) NOT NULL,
#     file_size INT,
#     content_type VARCHAR(100),
#     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
#     FOREIGN KEY (email_id) REFERENCES email_uploads(id) ON DELETE CASCADE,
#     INDEX idx_email_id (email_id)
# ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;